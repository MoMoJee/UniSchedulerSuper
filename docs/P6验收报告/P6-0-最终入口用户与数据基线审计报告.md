# P6-0 最终入口、用户与数据基线审计报告

> 日期：2026-07-13  
> 结论：通过，可进入 P6-A。

## 用户分类

- 数据库用户总数：39。
- verified clean：34；其中有历史 Planner source 的用户均具备完整 verified state，无历史数据账号按 `empty_new_user` 纳入 normalized 新写入口。
- retired quarantine：5，且仅为用户已批准的 `User1`、`test_user`、`User15`、`User21`、`User22`。
- unexpected blocking：0；分类互斥并覆盖全部用户。

## 数据基线

- Planner legacy `UserData`：157 行，2,788,568 bytes；逐行 manifest 只含 user/key/row id/bytes/SHA256，不含正文。
- `GroupCalendarData` 旧投影：7 行。
- 详细 normalized 实体/series/override/state/relation/change 计数见 `logs/p6-readiness.json`。
- readiness 在执行前后比较 UserData/state/issue/cohort/event/todo/reminder 计数，`read_only_verified=true`。

## 一致性门禁

```text
audit_planner_p6_readiness --strict: passed
verify_planner_migration --strict --exclude-retired-quarantine: 34 users, 0 diff
verify_recurrence_parity --strict --exclude-retired-quarantine: 34 users, 0 diff
test_p6_readiness: 1/1 passed
```

## 运行时入口清单

静态审计找到 21 个仍含 legacy repository/compat 或 `events_data` 符号的运行模块。归属如下：

- `migration.py`、`verification.py`、P6 readiness helper：改为仅离线工具可导入。
- `rollout.py`：P6-A 改为读取封存 manifest，不再逐请求读 legacy。
- Web/Agent/CalDAV/Feed 旧分支：P6-B 改 normalized adapter 或固定拒绝。
- `core/services/*` 和旧 RRule/分享 JSON 分支：P6-B 删除运行可达性，P6-F 在满足保留期后物理清理。
- model/admin 中字段声明不属于业务读写；P6-C 由数据库 trigger 阻止投影写入。

## 证据路径

- `logs/p6-readiness.json`
- `logs/p6-migration-parity-baseline.json`
- `logs/p6-recurrence-parity-baseline.json`
- `logs/p5-direct-userdata-final.json`

审计报告未包含 token、password、JSON 正文或完整 ICS。
