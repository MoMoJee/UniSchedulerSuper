# P6-F 前置门禁状态

> 检查日期：2026-07-13  
> 状态：未执行；这是预期门禁，不是代码失败。

P6-F 的文档前置条件是：P6-E 连续 7 天无未解释问题、至少跨过一个后续生产版本、存在额外离线备份。当前 cutover/观察开始于 2026-07-13 14:56，因此同一工作日不可能诚实满足该条件。

当前已具备：

- P6-0 至 P6-D 完成，P6-E 上线 smoke 通过。
- 切换前备份、演练副本、生产 archive manifest 和恢复材料完整。
- 运行时 legacy 已断路，数据库 trigger 已防写；旧源码/字段仅作为保留期恢复材料存在。

仍需满足：

1. 2026-07-20 14:56 之后确认连续 7 天 observation 报告无异常。
2. 完成至少一个后续生产版本发布。
3. 再创建一份离线备份并复验 checksum/恢复。
4. 用户另行批准任何 archive 或 `PlannerLegacyIdMap` 删除；默认不删。

门禁满足后才可删除旧 services/RRule backend、清空或删除 `GroupCalendarData.events_data` 字段，并重跑 178 项以上全量测试、strict/parity、恢复演练和数据库完整性。提前删除会违背既定升级方案并削弱回退材料。
