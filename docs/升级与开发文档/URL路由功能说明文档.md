# UniSchedulerSuper - URL 路由功能说明文档

> **文档版本**: v1.0  
> **创建日期**: 2025-11-07  
> **维护状态**: 持续更新中  
> **用途**: 记录所有 URL 路由的功能、参数、使用场景，便于后续开发和维护

---

## 📋 文档说明

本文档详细记录了 `core` 应用中所有 URL 路由的功能说明。路由按功能模块分类，便于查找和理解。

---

## 🎯 模块分类总览

- [页面路由模块](#页面路由模块) - 用户界面页面
- [Events 日程管理模块](#events-日程管理模块) - 日程相关操作
- [Reminders 提醒管理模块](#reminders-提醒管理模块) - 提醒相关操作
- [Todos 待办管理模块](#todos-待办管理模块) - 待办事项操作
- [用户设置模块](#用户设置模块) - 用户配置相关
- [其他功能模块](#其他功能模块) - 其他辅助功能

---

## 📄 页面路由模块

### 1. `/` - 首页

**视图函数**: `views.index`

**请求方式**: GET

**功能说明**:
- 应用的欢迎页面/着陆页
- 未登录用户的入口页面

**模板**: `index.html`

---

### 2. `/about/` - 关于页面

**视图函数**: `views.about`

**请求方式**: GET

**功能说明**:
- 显示应用的关于信息
- 自动读取 `core/static/about.md` 文件
- 将 Markdown 内容转换为 HTML 并渲染

**模板**: `about.html`

**特点**:
- 📝 支持 Markdown 格式
- 🔄 动态读取文件内容

---

### 3. `/home/` - 主应用页面

**视图函数**: `views.home`

**请求方式**: GET

**权限要求**: 需要登录 (`@login_required`)

**功能说明**:
- 应用的**核心工作页面**，包含日历视图
- 初始化用户必需的数据结构
- 传递日程组数据到前端

**初始化数据**:
1. `user_preference` - 用户偏好设置
2. `events_groups` - 日程组列表（默认4个组）
3. `planner` - AI 规划数据
4. `rrule_series_storage` - RRule 系列存储

**上下文数据**:
```python
{
    "datetime": "当前时间",
    "user": "当前用户对象",
    "events_groups": "日程组JSON数据"
}
```

**模板**: `home_new.html`

**特点**:
- 🎯 核心工作区域
- 🔄 自动初始化用户数据
- 📊 日历视图容器

---

### 4. `/user_register/` - 用户注册

**视图函数**: `views.user_register`

**请求方式**: GET, POST

**功能说明**:
- GET: 显示注册表单
- POST: 处理注册逻辑，创建新用户并自动登录

**表单**: `RegisterForm` (包含 username, email, password1, password2)

**成功后跳转**: `/home/` 主页面

**模板**: `user_register.html`

---

### 5. `/user_login/` - 用户登录

**视图函数**: `views.user_login`

**请求方式**: GET, POST

**功能说明**:
- GET: 显示登录表单
- POST: 验证用户凭据并登录

**表单**: Django 内置 `AuthenticationForm`

**成功后跳转**: `/home/` 主页面

**模板**: `user_login.html`

---

### 6. `/user_logout/` - 用户登出

**视图函数**: `views.user_logout`

**请求方式**: GET

**权限要求**: 需要登录

**功能说明**:
- 登出当前用户
- 重定向到首页

**跳转目标**: `/` 首页

---

### 7. `/contact/` - 联系页面

**视图函数**: `views.contact`

**请求方式**: GET

**权限要求**: 需要登录

**功能说明**:
- 显示联系信息或测试页面

**模板**: `contact.html`

**备注**: 代码注释标记为"测试"用途

---

### 8. `/help/` - 帮助文档页面

**视图函数**: `views.help_page`

**请求方式**: GET

**权限要求**: 需要登录

**功能说明**:
- 显示应用的帮助文档和使用说明

**模板**: `help.html`

---

### 9. `/user_data/` - 用户数据查看

**视图函数**: `views.user_data`

**请求方式**: GET

**权限要求**: 需要登录

**功能说明**:
- 显示当前用户的所有 UserData 记录
- 用于调试和查看用户存储的数据

**上下文数据**:
```python
{
    "user_data": "当前用户的所有 UserData 对象列表"
}
```

**模板**: `user_data.html`

---

## 📅 Events 日程管理模块

### 1. `/get_calendar/update_events/` - 快速更新日程

**视图函数**: `views.update_events` → `views_events.update_events_impl`

**请求方式**: POST

**功能说明**:
- 用于**拖拽移动**或**调整大小**时的快速更新
- 用于**简单编辑框**中的基本字段修改
- 支持**单次事件转重复事件**（添加 RRule）

**请求参数**:
```json
{
    "eventId": "事件ID",
    "title": "标题",
    "newStart": "新的开始时间 (ISO格式)",
    "newEnd": "新的结束时间 (ISO格式)",
    "description": "描述",
    "importance": "important/not-important",
    "urgency": "urgent/not-urgent",
    "groupID": "日程组ID",
    "ddl": "截止时间",
    "rrule": "重复规则 (可选)",
    "rrule_change_scope": "single/all/future/from_time (仅重复事件)"
}
```

**使用场景**:
- ✅ 拖拽移动单个日程到新时间
- ✅ 调整日程的持续时间
- ✅ 编辑框中修改日程基本信息
- ✅ 将单次日程转换为重复日程

**重复事件处理**:
- `single`: 仅修改单个实例（从系列中独立，添加 EXDATE）
- `all`: 修改整个系列
- `future`: 修改当前及之后的实例
- `from_time`: 从指定时间开始修改

**响应示例**:
```json
{
    "status": "success"
}
```

---

### 2. `/api/events/bulk-edit/` - 批量编辑日程

**视图函数**: `views_events.bulk_edit_events_impl`

**请求方式**: POST

**功能说明**:
- 用于**批量编辑**重复日程
- 用于**删除操作**（单个/系列/此及之后）
- 支持**修改 RRule 规则**（改变重复模式）
- 支持**结束重复系列**（将 RRule 改为空）

**请求参数**:
```json
{
    "operation": "edit/delete",
    "event_id": "事件ID",
    "edit_scope": "single/all/future/from_time",
    "series_id": "系列ID (重复事件必需)",
    "from_time": "起始时间 (scope=from_time 时必需)",
    
    // 以下为编辑操作时的更新数据
    "title": "标题",
    "description": "描述",
    "importance": "重要性",
    "urgency": "紧急性",
    "start": "开始时间",
    "end": "结束时间",
    "rrule": "新的重复规则",
    "groupID": "日程组ID",
    "ddl": "截止时间"
}
```

**使用场景**:
- ✅ 删除单个日程实例
- ✅ 删除整个重复系列
- ✅ 删除"此日程及之后"的所有实例
- ✅ 批量修改重复日程的属性
- ✅ 修改重复规则（如从每周改为每月）
- ✅ 取消重复（将重复日程改为单次）

**删除操作 (operation: "delete")**:
- `single`: 删除单个实例（使用 EXDATE 机制防止重新生成）
- `all`: 删除整个重复系列
- `future`: 删除此及之后（使用 UNTIL 截断）

**编辑操作 (operation: "edit")**:
- `single`: 编辑单个实例（从系列中独立）
- `all`: 编辑整个系列
- `future`/`from_this`: 从某时间点开始编辑
- 支持 RRule 变化检测（自动判断是否需要创建新系列）

**特殊功能**:
- ⏱️ **超时控制**: 35 秒超时，25 秒警告返回
- 🔄 **主事件转移**: 删除主事件时自动提升下一个实例
- 🎯 **智能 DDL**: 实例的 DDL 自动同步主事件时间
- 🚫 **EXDATE 管理**: 删除后添加例外，防止自动重新生成

**响应示例**:
```json
{
    "status": "success",
    "deleted_count": 5,  // 删除操作时返回
    "updated_count": 10  // 编辑操作时返回
}
```

---

### 功能对比表

| 功能 | update_events | bulk-edit | 推荐使用 |
|------|---------------|-----------|----------|
| 拖拽移动单个日程 | ✅ 快速 | ✅ 支持 | **update_events** |
| 修改单个日程字段 | ✅ 简单 | ✅ 支持 | **update_events** |
| 删除单个日程 | ❌ | ✅ | **bulk-edit** |
| 删除整个系列 | ❌ | ✅ | **bulk-edit** |
| 批量编辑重复日程 | 部分支持 | ✅ 完整 | **bulk-edit** |
| 修改 RRule 规则 | ❌ | ✅ | **bulk-edit** |
| 单次转重复 | ✅ | ✅ | **update_events** |

---

### 3. `/get_calendar/events/` - 获取所有日程

**视图函数**: `views.get_events` → `views_events.get_events_impl`

**请求方式**: GET

**权限要求**: 需要登录

**功能说明**:
- 获取当前用户的所有日程事件
- 同时获取所有日程组信息
- 自动处理重复日程的实例生成
- 新用户自动创建欢迎日程

**返回数据**:
```json
{
    "events": [
        {
            "id": "事件ID",
            "title": "标题",
            "start": "开始时间",
            "end": "结束时间",
            "description": "描述",
            "importance": "重要性",
            "urgency": "紧急性",
            "groupID": "日程组ID",
            "ddl": "截止时间",
            "rrule": "重复规则",
            "series_id": "系列ID",
            "is_recurring": true/false,
            "is_main_event": true/false
        }
    ],
    "events_groups": [
        {
            "id": "组ID",
            "name": "组名",
            "description": "描述",
            "color": "颜色"
        }
    ]
}
```

**特点**:
- 🔄 自动生成重复日程实例
- 📊 一次性返回日程和日程组
- 🎁 新用户欢迎日程

---

### 4. `/events/create_event/` - 创建新日程

**视图函数**: `views.create_event` → `views_events.create_event_impl`

**请求方式**: POST

**权限要求**: 需要登录

**功能说明**:
- 创建单次日程
- 创建重复日程（带 RRule）
- 自动生成重复日程的实例
- 支持自动 DDL 设置

**请求参数**:
```json
{
    "title": "标题 (必填)",
    "start": "开始时间 (必填, ISO格式)",
    "end": "结束时间 (必填, ISO格式)",
    "description": "描述",
    "importance": "important/not-important",
    "urgency": "urgent/not-urgent",
    "groupID": "日程组ID",
    "ddl": "截止时间 (可选)",
    "rrule": "重复规则 (可选, 如: FREQ=DAILY;INTERVAL=1)"
}
```

**重复规则处理**:
- **无 COUNT/UNTIL**: 自动生成适量实例
  - 月度重复: 36 个实例（3年）
  - 周度重复: 26 个实例（半年）
  - 日度重复: 20 个实例（90天）
- **有 COUNT**: 生成指定数量的实例
- **有 UNTIL**: 生成到截止时间的所有实例

**复杂 RRule 处理**:
- 自动查找下一个符合条件的时间点（BYDAY, BYMONTHDAY 等）
- 保留用户指定的时分秒

**响应示例**:
```json
{
    "status": "success",
    "event": {
        "id": "新创建的事件ID",
        "series_id": "系列ID (重复事件)",
        ...
    }
}
```

---

### 5. `/get_calendar/create_events_group/` - 创建日程组

**视图函数**: `views.create_events_group`

**请求方式**: POST

**权限要求**: 需要登录

**功能说明**:
- 创建新的日程分组
- 用于组织和分类日程

**请求参数**:
```json
{
    "name": "组名 (必填)",
    "description": "描述",
    "color": "颜色代码 (如: #3498db)"
}
```

**响应示例**:
```json
{
    "status": "success"
}
```

---

### 6. `/get_calendar/update_events_group/` - 更新日程组

**视图函数**: `views.update_event_group`

**请求方式**: POST

**权限要求**: 需要登录

**功能说明**:
- 修改已存在的日程组信息

**请求参数**:
```json
{
    "groupID": "组ID (必填)",
    "title": "新组名",
    "description": "新描述",
    "color": "新颜色"
}
```

**响应示例**:
```json
{
    "status": "success"
}
```

---

### 7. `/get_calendar/delete_event_groups/` - 删除日程组

**视图函数**: `views.delete_event_groups`

**请求方式**: POST

**权限要求**: 需要登录

**功能说明**:
- 批量删除日程组
- 可选择是否同时删除组内日程

**请求参数**:
```json
{
    "groupIds": ["组ID1", "组ID2", ...],
    "deleteEvents": true/false  // 是否删除组内的所有日程
}
```

**特点**:
- 📦 支持批量删除
- ⚠️ 可控制是否级联删除日程
- 🛡️ 安全性：需要明确指定是否删除日程

**响应示例**:
```json
{
    "status": "success"
}
```

---

### 8. `/get_calendar/import_events/` - 导入日程（教务系统）

**视图函数**: `views.import_events`

**请求方式**: POST

**权限要求**: 需要登录

**功能说明**:
- 从民族大学教务系统导入课程表
- 自动转换为日程格式
- 导入到指定日程组

**请求参数**:
```json
{
    "cookie": "教务系统 Cookie (必填)",
    "groupId": "目标日程组ID (必填)"
}
```

**处理流程**:
1. 使用 Cookie 爬取教务系统数据
2. 解析课程信息为日程格式
3. 设置所有课程的 groupID 为目标组
4. 合并到用户的日程列表

**响应示例**:
```json
{
    "status": "success"
}
```

**错误处理**:
- Cookie 无效或过期
- 教务系统无法访问
- 数据解析失败

---

### 9. `/get_calendar/outport_calendar/` - 导出日历页面

**视图函数**: `views.outport_calendar`

**请求方式**: GET

**权限要求**: 需要登录

**功能说明**:
- 显示日历导出配置页面
- 用户可选择导出模式和选项

**模板**: `outport_calendar.html`

---

### 10. `/get_calendar/get_outport_calendar/` - 导出 iCal 文件

**视图函数**: `views.get_outport_calendar`

**请求方式**: GET

**权限要求**: 需要登录

**功能说明**:
- 生成 iCalendar (.ics) 格式文件
- 支持两种导出模式
- 跟踪已导出的日程，避免重复

**请求参数**:
```
?mode=new|all        # 导出模式
&user_choice=1|2     # 对已修改日程的处理方式（mode=new时）
```

**导出模式**:

**模式一: `mode=new`** (增量导出)
- 只导出新增和修改的日程
- 跟踪上次导出时间
- 检测已修改的日程，让用户选择：
  - `user_choice=1`: 包含修改的日程
  - `user_choice=2`: 忽略修改的日程

**模式二: `mode=all`** (全量导出)
- 导出所有日程
- 更新导出记录

**返回格式**:
```
Content-Type: text/calendar
Content-Disposition: attachment; filename=calendar.ics
```

**特点**:
- 📅 标准 iCal 格式，兼容所有日历应用
- 🔄 智能增量导出
- 📊 跟踪导出历史

---

### 11. `/get_calendar/check_modified_events/` - 检查修改的日程

**视图函数**: `views.check_modified_events`

**请求方式**: GET

**权限要求**: 需要登录

**功能说明**:
- 检查自上次导出后哪些日程被修改
- 配合 `get_outport_calendar` 使用

**请求参数**:
```
?mode=new
```

**返回数据**:
```json
{
    "modified_events": [
        {
            "title": "日程标题",
            "description": "日程描述"
        }
    ]
}
```

**用途**:
- 在增量导出前预览修改内容
- 帮助用户决定是否包含修改的日程

---

### 12. `/get_calendar/delete_event/` - 删除日程 (已弃用)

**视图函数**: `views.delete_event` → `views_events.delete_event_impl`

**请求方式**: POST

**权限要求**: 需要登录

**状态**: ⚠️ **几乎已弃用**

**功能说明**:
- 旧版删除日程接口
- 现已被 `/api/events/bulk-edit/` 替代

**推荐**: 使用 `/api/events/bulk-edit/` 的 delete 操作

---

### 13. `/get_calendar/resources/` - 获取资源列表

**视图函数**: `views.get_resources`

**请求方式**: GET

**功能说明**:
- 获取日历资源配置（如会议室、设备）
- 用于 FullCalendar 的资源视图功能

**返回数据示例**:
```json
[
    {
        "id": "a",
        "title": "Auditorium A",
        "occupancy": 40
    },
    {
        "id": "d",
        "title": "Auditorium D",
        "occupancy": 40,
        "children": [
            { "id": "d1", "title": "Room D1", "occupancy": 10 },
            { "id": "d2", "title": "Room D2", "occupancy": 10 }
        ]
    }
]
```

**特点**:
- 📍 支持层级结构（父子资源）
- 🎨 可为资源设置颜色
- 👥 记录容量信息

**用途**:
- 会议室预订系统
- 资源调度可视化
- （当前为示例数据，可扩展）

---

## 🔔 Reminders 提醒管理模块

### 1. `/api/reminders/` - 获取所有提醒

**视图函数**: `views.get_reminders` → `views_reminder.get_reminders_impl`

**请求方式**: GET

**功能说明**:
- 获取当前用户的所有提醒
- 自动生成缺失的重复提醒实例
- 返回包括单次和重复提醒

**返回数据**:
```json
{
    "reminders": [
        {
            "id": "提醒ID",
            "title": "标题",
            "content": "内容",
            "trigger_time": "触发时间",
            "priority": "优先级",
            "status": "状态",
            "rrule": "重复规则",
            "series_id": "系列ID",
            "is_recurring": true/false
        }
    ]
}
```

**特点**:
- 🔄 自动生成重复提醒实例（未来30天内）
- 📊 一次性返回所有类型的提醒

---

### 2. `/api/reminders/create/` - 创建新提醒

**视图函数**: `views.create_reminder` → `views_reminder.create_reminder_impl`

**请求方式**: POST

**功能说明**:
- 创建单次提醒
- 创建重复提醒（带 RRule）
- 自动生成重复提醒实例

**请求参数**:
```json
{
    "title": "标题 (必填)",
    "content": "内容",
    "trigger_time": "触发时间 (必填, ISO格式)",
    "priority": "low/normal/high/critical (默认: normal)",
    "rrule": "重复规则 (可选)"
}
```

**重复规则示例**:
```
FREQ=DAILY;INTERVAL=1              // 每天
FREQ=WEEKLY;BYDAY=MO,WE,FR         // 每周一、三、五
FREQ=MONTHLY;BYMONTHDAY=15         // 每月15日
FREQ=MONTHLY;BYDAY=2MO             // 每月第2个星期一
```

**响应示例**:
```json
{
    "status": "success",
    "message": "重复提醒已创建，系列ID: abc-123"
}
```

---

### 3. `/api/reminders/update/` - 更新提醒

**视图函数**: `views.update_reminder` → `views_reminder.update_reminder_impl`

**请求方式**: POST

**功能说明**:
- 更新单次提醒的信息
- **支持普通提醒转重复提醒**（添加 RRule）
- 不支持复杂的重复提醒修改（使用 bulk-edit）

**请求参数**:
```json
{
    "id": "提醒ID (必填)",
    "title": "新标题",
    "content": "新内容",
    "trigger_time": "新触发时间",
    "priority": "新优先级",
    "rrule": "重复规则 (转重复提醒时使用)",
    "rrule_change_scope": "all (转重复时必需)"
}
```

**使用场景**:
- ✅ 修改提醒的基本信息
- ✅ 将单次提醒转为重复提醒
- ❌ 修改重复提醒的规则（使用 `/api/reminders/bulk-edit/`）

---

### 4. `/api/reminders/update-status/` - 更新提醒状态

**视图函数**: `views.update_reminder_status` → `views_reminder.update_reminder_status_impl`

**请求方式**: POST

**功能说明**:
- 更新提醒的状态（激活/完成/忽略/延后）
- 快速状态切换，不修改其他信息

**请求参数**:
```json
{
    "id": "提醒ID (必填)",
    "status": "active/completed/dismissed/snoozed (必填)",
    "snooze_until": "延后到的时间 (status=snoozed时)"
}
```

**状态说明**:
- `active`: 激活状态
- `completed`: 已完成
- `dismissed`: 已忽略/关闭
- `snoozed`: 已延后

**响应示例**:
```json
{
    "status": "success"
}
```

---

### 5. `/api/reminders/delete/` - 删除提醒

**视图函数**: `views.delete_reminder` → `views_reminder.delete_reminder_impl`

**请求方式**: POST

**功能说明**:
- 删除单个提醒
- 对于重复提醒，建议使用 `/api/reminders/bulk-edit/`

**请求参数**:
```json
{
    "id": "提醒ID (必填)"
}
```

**响应示例**:
```json
{
    "status": "success"
}
```

---

### 6. `/api/reminders/bulk-edit/` - 批量编辑重复提醒

**视图函数**: `views.bulk_edit_reminders` → `views_reminder.bulk_edit_reminders_impl`

**请求方式**: POST

**功能说明**:
- 批量编辑重复提醒
- 批量删除重复提醒
- 修改重复规则
- 取消重复（结束重复系列）

**请求参数**:
```json
{
    "operation": "edit/delete",
    "reminder_id": "提醒ID",
    "edit_scope": "this_only/all/from_this/from_time",
    "series_id": "系列ID",
    "from_time": "起始时间 (scope=from_time时)",
    
    // 编辑操作时的更新数据
    "title": "新标题",
    "content": "新内容",
    "priority": "新优先级",
    "trigger_time": "新触发时间",
    "rrule": "新重复规则"
}
```

**编辑范围**:
- `this_only`: 仅此提醒（从系列中独立）
- `all`: 整个系列
- `from_this`: 此提醒及之后
- `from_time`: 从指定时间开始

**删除操作**:
- `this_only`: 删除单个实例（添加 EXDATE）
- `all`: 删除整个系列
- `from_this`: 删除此及之后（添加 UNTIL）

**特点**:
- 🔄 支持 RRule 修改（自动创建新系列）
- 🚫 支持取消重复（RRule 改为空）
- ⏱️ 超时保护机制

---

### 7. `/api/reminders/convert-to-single/` - 重复转单次

**视图函数**: `views.convert_recurring_to_single` → `views_reminder.convert_recurring_to_single_impl`

**请求方式**: POST

**功能说明**:
- 将重复提醒的某个实例转换为单次提醒
- 保留当前实例，删除未来实例
- 为过去实例添加 UNTIL 限制

**请求参数**:
```json
{
    "series_id": "系列ID (必填)",
    "reminder_id": "目标提醒ID (必填)",
    "update_data": {
        "title": "新标题",
        "content": "新内容",
        ...
    }
}
```

**处理逻辑**:
1. 将目标提醒转为单次提醒（清除 RRule）
2. 删除所有未来的重复实例
3. 为过去的实例添加 UNTIL 截止时间

**响应示例**:
```json
{
    "status": "success",
    "message": "重复提醒已成功转换为单次提醒",
    "deleted_count": 5,
    "updated_count": 3
}
```

---

### 8. `/api/reminders/pending/` - 获取待触发提醒

**视图函数**: `views.get_pending_reminders` → `views_reminder.get_pending_reminders_impl`

**请求方式**: GET

**功能说明**:
- 获取即将触发或应该触发的提醒
- 用于前端通知系统轮询

**返回数据**:
```json
{
    "pending_reminders": [
        {
            "id": "提醒ID",
            "title": "标题",
            "trigger_time": "触发时间",
            "priority": "优先级",
            ...
        }
    ]
}
```

**触发条件**:
- 提醒状态为 `active`
- 触发时间 <= 当前时间 + 1分钟

---

### 9. `/api/reminders/maintain/` - 维护提醒实例

**视图函数**: `views.maintain_reminders` → `views_reminder.maintain_reminders_impl`

**请求方式**: POST

**功能说明**:
- 定期调用以维护重复提醒实例
- 自动生成缺失的未来实例
- 确保提醒实例充足

**返回数据**:
```json
{
    "status": "success",
    "original_count": 50,
    "updated_count": 65,
    "added_instances": 15
}
```

**用途**:
- 后台定时任务
- 手动刷新提醒列表

---

### 10-12. 快捷操作 API（已弃用）

以下三个 URL 在代码注释中标记为"貌似没有用法了"，建议使用更通用的 `update-status` 接口：

#### `/api/reminders/snooze/` - 延后提醒 ⚠️
**替代方案**: 使用 `update-status` 并设置 `status: "snoozed"`

#### `/api/reminders/dismiss/` - 忽略提醒 ⚠️
**替代方案**: 使用 `update-status` 并设置 `status: "dismissed"`

#### `/api/reminders/complete/` - 完成提醒 ⚠️
**替代方案**: 使用 `update-status` 并设置 `status: "completed"`

---

## ✅ Todos 待办管理模块

### 1. `/api/todos/` - 获取所有待办

**视图函数**: `views.get_todos`

**请求方式**: GET

**功能说明**:
- 获取当前用户的所有待办事项

**返回数据**:
```json
{
    "todos": [
        {
            "id": "待办ID",
            "title": "标题",
            "description": "描述",
            "due_date": "截止日期",
            "estimated_duration": "预估时长",
            "importance": "重要性",
            "urgency": "紧急性",
            "groupID": "关联组ID",
            "status": "pending/in-progress/completed/converted",
            "created_at": "创建时间",
            "last_modified": "修改时间"
        }
    ]
}
```

---

### 2. `/api/todos/create/` - 创建待办

**视图函数**: `views.create_todo`

**请求方式**: POST

**功能说明**:
- 创建新的待办事项

**请求参数**:
```json
{
    "title": "标题 (必填)",
    "description": "描述",
    "due_date": "截止日期",
    "estimated_duration": "预估时长 (分钟)",
    "importance": "important/not-important",
    "urgency": "urgent/not-urgent",
    "groupID": "关联组ID"
}
```

**响应示例**:
```json
{
    "status": "success",
    "todo": {
        "id": "新创建的待办ID",
        ...
    }
}
```

---

### 3. `/api/todos/update/` - 更新待办

**视图函数**: `views.update_todo`

**请求方式**: POST

**功能说明**:
- 更新待办事项的信息

**请求参数**:
```json
{
    "id": "待办ID (必填)",
    "title": "新标题",
    "description": "新描述",
    "due_date": "新截止日期",
    "estimated_duration": "新预估时长",
    "importance": "新重要性",
    "urgency": "新紧急性",
    "groupID": "新关联组",
    "status": "新状态"
}
```

**状态值**:
- `pending`: 待处理
- `in-progress`: 进行中
- `completed`: 已完成
- `converted`: 已转为日程

**响应示例**:
```json
{
    "status": "success"
}
```

---

### 4. `/api/todos/delete/` - 删除待办

**视图函数**: `views.delete_todo`

**请求方式**: POST

**功能说明**:
- 删除指定的待办事项

**请求参数**:
```json
{
    "id": "待办ID (必填)"
}
```

**响应示例**:
```json
{
    "status": "success"
}
```

---

### 5. `/api/todos/convert/` - 待办转日程

**视图函数**: `views.convert_todo_to_event`

**请求方式**: POST

**功能说明**:
- 将待办事项转换为日程事件
- 自动继承待办的属性
- 标记待办为"已转换"状态

**请求参数**:
```json
{
    "id": "待办ID (必填)",
    "start_time": "开始时间 (必填, ISO格式)",
    "end_time": "结束时间 (必填, ISO格式)"
}
```

**转换逻辑**:
1. 创建新日程，继承待办的：
   - 标题、描述
   - 重要性、紧急性
   - 关联组ID
   - 截止日期 → DDL
2. 标记待办状态为 `converted`
3. 在待办中记录转换后的日程ID

**响应示例**:
```json
{
    "status": "success",
    "event": {
        "id": "新创建的日程ID",
        "converted_from_todo": "原待办ID",
        ...
    }
}
```

**特点**:
- 🔗 双向关联：待办和日程互相记录ID
- 📊 保持一致性：属性完整继承
- 🎯 状态追踪：可查看哪些待办已转为日程

---

## ⚙️ 用户设置模块

### 1. `/change_view/` - 保存界面设置

**视图函数**: `views.change_view`

**请求方式**: POST

**功能说明**:
- 保存用户的界面状态和偏好
- 存储视图模式、筛选器状态、面板布局等

**请求参数**:
```json
{
    "user_interface_settings": {
        "todoViewMode": "list",         // 待办视图模式 (list/kanban/timeline)
        "todoFilters": {                // 待办筛选器状态
            "status": "all",            // all/pending/completed
            "priority": "all",          // all/high/medium/low
            "urgency": "all"            // all/urgent/not-urgent
        },
        "reminderFilters": {            // 提醒筛选器状态
            "status": "active",         // active/all/completed
            "type": "all"               // all/single/recurring
        },
        "calendarFilters": {            // 日历筛选器状态
            "categories": ["work", "personal"],  // 显示的分类
            "showCompleted": true       // 是否显示已完成
        },
        "calendarView": "dayGridMonth", // 日历视图 (dayGridMonth/timeGridWeek/timeGridDay)
        "panelLayout": {                // 面板布局
            "leftPanelWidth": 250,
            "rightPanelWidth": 300,
            "mainContentWidth": 1000
        }
    }
}
```

**响应示例**:
```json
{
    "status": "success",
    "message": "界面设置已保存"
}
```

**使用场景**:
- 🎨 用户切换视图模式时自动保存
- 🔍 调整筛选器状态后持久化
- 📐 拖拽调整面板宽度后保存
- 🔄 刷新页面后恢复用户的界面状态

**数据存储**: 保存到 `UserData` 的 `user_interface_settings` 键中

---

### 2. `/get_calendar/user_preferences/` - 用户偏好设置页面

**视图函数**: `views.user_preferences`

**请求方式**: GET, POST

**功能说明**:
- **GET**: 渲染用户偏好设置页面
- **POST**: 保存用户偏好配置

---

#### GET - 显示设置页面

**模板**: `user_preferences.html`

**功能**: 展示偏好设置表单，用户可修改：
- 周数显示设置
- 工作时间配置
- 默认日程时长
- 日历默认视图
- 主题选择

---

#### POST - 保存偏好设置

**请求参数** (Form Data):
```json
{
    "week_number_start": {"month": 2, "day": 24},   // 旧版周数起始（已弃用）
    "week_number_periods": [                        // 多学期配置
        {"name": "春季学期", "month": 2, "day": 24},
        {"name": "秋季学期", "month": 9, "day": 1}
    ],
    "show_week_number": true,                       // 是否显示周数
    "auto_ddl": true,                               // 自动设置DDL
    "default_event_duration": 60,                   // 默认日程时长 (分钟)
    "work_hours_start": "09:00",                    // 工作开始时间
    "work_hours_end": "18:00",                      // 工作结束时间
    "show_weekends": true,                          // 显示周末
    "week_starts_on": 1,                            // 周起始 (0=周日, 1=周一)
    "calendar_view_default": "dayGridMonth",        // 默认日历视图
    "theme": "light"                                // 主题 (light/dark)
}
```

**响应**: 重定向回偏好设置页面

**特点**:
- ⏰ **时区转换**: 使用 `add_8_hours_to_time_data()` 自动处理时区（+8小时）
- 📊 **数据验证**: 使用 `DATA_SCHEMA` 确保数据格式正确
- 🔧 **持久化存储**: 保存到 `UserData` 的 `user_preference` 键中

**时区转换说明**:
```python
# 用户输入 09:00 → 存储为 17:00 (加8小时)
# 用户输入 18:00 → 存储为 02:00 (加8小时)
```

---

### 3. `/api/user/settings/` - 用户设置 API

**视图函数**: `views.user_settings`

**请求方式**: GET, POST

**功能说明**:
- **GET**: 获取用户偏好设置 JSON
- **POST**: 异步保存用户设置

---

#### GET - 获取设置

**功能**:
- 返回完整的用户偏好设置
- 提供默认值（新用户）
- 自动数据迁移

**返回数据**:
```json
{
    "week_number_start": {"month": 2, "day": 24},
    "week_number_periods": [
        {"name": "默认学期", "month": 2, "day": 24}
    ],
    "show_week_number": true,
    "auto_ddl": true,
    "default_event_duration": 60,
    "work_hours_start": "09:00",
    "work_hours_end": "18:00",
    "show_weekends": true,
    "week_starts_on": 1,
    "calendar_view_default": "dayGridMonth",
    "theme": "light",
    "show_completed_events": true,
    "event_color_by": "default",
    "reminder_enabled": true,
    "default_reminder_time": 15,
    "reminder_sound": true,
    "ai_enabled": true,
    "ai_auto_suggest": false
}
```

**自动数据迁移**:
- 检测旧版 `week_number_start` 字段
- 自动转换为新版 `week_number_periods` 数组格式
- 保存迁移后的数据

**默认值说明**:
- 如果用户从未设置，返回预定义的默认配置
- 确保前端始终有完整的配置数据

---

#### POST - 保存设置

**请求参数** (JSON):
```json
{
    // 同 GET 返回的数据结构
    "theme": "dark",
    "show_week_number": false,
    "auto_ddl": false,
    ...
}
```

**响应示例**:
```json
{
    "status": "success",
    "message": "设置已保存"
}
```

**错误响应**:
```json
{
    "status": "error",
    "message": "获取设置失败: 错误详情"
}
```

**特点**:
- ✅ **数据验证**: 使用 `DATA_SCHEMA` 验证 (`check=True`)
- 🔄 **向下兼容**: 自动处理数据格式升级
- 🎯 **前端友好**: JSON 格式，适合 AJAX 调用

**使用场景**:
- 📱 前端 JavaScript 异步获取/保存设置
- 🔄 页面加载时初始化用户配置
- ⚙️ 设置页面实时保存

**与其他接口的区别**:
- `/get_calendar/user_preferences/`: 页面渲染 + 表单提交（传统方式）
- `/api/user/settings/`: JSON API（现代 AJAX 方式）
- `/change_view/`: 仅保存界面状态，不包含核心偏好设置

---

## 🔧 其他功能模块

### 1. `/api/reminders/mark-sent/` - 标记通知已发送

**视图函数**: `views.mark_notification_sent`

**请求方式**: POST

**功能说明**:
- 标记提醒通知已发送给用户
- 占位函数，用于记录通知发送历史

**请求参数**:
```json
{
    "reminder_id": "提醒ID",
    "sent_time": "发送时间"
}
```

**响应示例**:
```json
{
    "status": "success"
}
```

**状态**: 📝 占位实现
- 当前仅返回成功状态
- TODO: 将来可以记录通知发送历史，用于统计和审计

**使用场景**:
- 通知系统发送提醒后调用
- 记录用户收到通知的时间
- 避免重复发送通知

---

### 2. `/friendly_link/` - 友情链接页面

**视图函数**: `views.friendly_link`

**请求方式**: GET

**功能说明**:
- 显示友情链接页面
- 展示合作伙伴或相关网站

**模板**: `memory.html`

**说明**: 
- 🤝 用于展示友情链接
- 📜 模板名为 `memory.html`（可能是历史原因）

---

### 3. `/three_body/` - 三体彩蛋页面

**视图函数**: `views.three_body`

**请求方式**: GET

**功能说明**:
- 显示三体主题彩蛋页面
- 可能包含特殊动画或主题

**模板**: `three_body.html`

**说明**: 
- 🎨 特殊主题页面
- 🎁 可能是项目的彩蛋或特色功能

---

### 4. `/help/` - 帮助文档页面

**视图函数**: `views.help_page`

**请求方式**: GET

**功能说明**:
- 显示用户帮助文档
- 提供功能使用指南

**模板**: `help.html`

**说明**: 
- 📖 用户帮助中心
- 💡 功能说明和使用教程

---

### 5. `/user_data/` - 用户数据管理页面

**视图函数**: `views.user_data`

**请求方式**: GET

**权限要求**: 需要登录

**功能说明**:
- 显示用户的所有 UserData 记录
- 用于调试和数据管理

**模板**: `user_data.html`

**返回数据**:
- 查询当前用户的所有 `UserData` 对象
- 展示所有 key-value 数据

**使用场景**:
- 🔍 开发调试：查看用户数据结构
- 🛠️ 数据管理：检查存储的配置
- 📊 数据审计：了解用户设置的完整状态

---

## 📊 URL 统计总结

### 按模块分类

| 模块 | URL 数量 | 说明 |
|------|----------|------|
| 📄 页面路由 | 9 | 首页、关于、登录注册等页面 |
| 📅 Events 日程管理 | 13 | 日程 CRUD、日程组管理、导入导出 |
| 🔔 Reminders 提醒管理 | 12 | 提醒 CRUD、重复提醒、状态管理 |
| ✅ Todos 待办管理 | 5 | 待办 CRUD、转换为日程 |
| ⚙️ 用户设置 | 3 | 界面设置、偏好配置 API |
| 🔧 其他功能 | 5 | 通知标记、帮助页面、彩蛋 |
| **总计** | **47** | |

### 按功能类型分类

| 类型 | URL 数量 |
|------|----------|
| 🌐 页面渲染 (GET) | 15 |
| 📡 数据 API (GET) | 7 |
| ✏️ 创建/更新 (POST/PUT) | 16 |
| 🗑️ 删除 (POST/DELETE) | 4 |
| 🔄 批量操作 (POST) | 3 |
| 🔁 状态切换 (POST) | 2 |

### 重要 API 速查

**最常用的 API**:
- `GET /get_calendar/events/` - 获取所有日程
- `POST /get_calendar/update_events/` - 更新日程（快速）
- `POST /api/events/bulk-edit/` - 批量编辑日程（复杂）
- `GET /api/reminders/` - 获取所有提醒
- `POST /api/reminders/bulk-edit/` - 批量编辑提醒
- `GET /api/todos/` - 获取所有待办
- `GET /api/user/settings/` - 获取用户设置

**已弃用的 API**:
- `/get_calendar/delete_event/` - 使用 `/api/events/bulk-edit/` 替代
- `/api/reminders/snooze/` - 使用 `/api/reminders/update-status/` 替代
- `/api/reminders/dismiss/` - 使用 `/api/reminders/update-status/` 替代
- `/api/reminders/complete/` - 使用 `/api/reminders/update-status/` 替代

---

## 🎯 使用建议

### 日程操作

**快速更新单个日程**:
```javascript
POST /get_calendar/update_events/
{
    "id": "event-123",
    "start": "2025-01-20T10:00:00",
    "end": "2025-01-20T11:00:00"
}
```

**复杂批量操作**:
```javascript
POST /api/events/bulk-edit/
{
    "operation": "edit",
    "event_id": "event-123",
    "edit_scope": "all",
    "title": "新标题",
    "rrule": "FREQ=WEEKLY;BYDAY=MO,WE"
}
```

### 提醒操作

**创建重复提醒**:
```javascript
POST /api/reminders/create/
{
    "title": "每天喝水",
    "trigger_time": "2025-01-20T09:00:00",
    "rrule": "FREQ=DAILY;INTERVAL=1"
}
```

**修改提醒状态**:
```javascript
POST /api/reminders/update-status/
{
    "id": "reminder-456",
    "status": "completed"
}
```

### 设置保存

**保存界面状态**:
```javascript
POST /change_view/
{
    "user_interface_settings": {
        "calendarView": "timeGridWeek",
        "panelLayout": { "leftPanelWidth": 300 }
    }
}
```

**保存用户偏好**:
```javascript
POST /api/user/settings/
{
    "theme": "dark",
    "auto_ddl": true,
    "work_hours_start": "09:00"
}
```

---

## 📝 更新日志

- **2025-01-07**: 
  - ✅ 创建文档结构
  - ✅ 完成页面路由模块分析（9个URL）
  - ✅ 完成 Events 日程管理模块分析（13个URL）
  - ✅ 完成 Reminders 提醒管理模块分析（12个URL）
  - ✅ 完成 Todos 待办管理模块分析（5个URL）
  - ✅ 完成用户设置模块分析（3个URL）
  - ✅ 完成其他功能模块分析（5个URL）
  - ✅ 添加统计总结和使用建议
  - 📊 **总计分析 47 个 URL**

---

## 📌 文档说明

### 文档目的
本文档详细记录了 `core` 应用的所有 URL 路由及其功能，包括：
- 请求参数和响应格式
- 使用场景和最佳实践
- API 之间的关系和区别
- 已弃用的接口和替代方案

### 维护建议
- ✏️ 添加新 URL 时，请同步更新此文档
- 🔄 修改 API 行为时，更新对应章节
- ⚠️ 标记弃用的 API 并提供替代方案
- 📊 定期更新统计数据

### 相关文档
- `开发文档.md` - 项目整体架构和功能说明
- `models.py` - 数据模型定义
- `views_events.py` - Events 模块实现
- `views_reminder.py` - Reminders 模块实现
- `RRule功能说明.md` - 重复事件引擎文档

---

## 🎉 文档完成

本文档已完整分析了 `core/urls.py` 中的所有 47 个 URL 路由。
如有问题或需要补充，请随时更新。

**最后更新**: 2025-01-07
**维护者**: MoMoJee
**版本**: v1.0

