# 邮件验证功能 - 快速配置指南

## 🚀 5分钟快速启动

### 步骤1: 配置邮件服务

```bash
# 复制示例配置文件
cp config/email.example.json config/email.json
```

编辑 `config/email.json`，填入你的SMTP配置：

```json
{
  "enabled": true,
  "smtp_host": "smtpdm.aliyun.com",
  "smtp_port": 465,
  "smtp_use_ssl": true,
  "smtp_user": "verify@unischedulersuper.online",
  "smtp_password": "你的SMTP密码",
  "from_email": "verify@unischedulersuper.online",
  "from_name": "UniSchedulerSuper"
}
```

### 步骤2: 生成数据库迁移

```bash
python manage.py makemigrations
python manage.py migrate
```

### 步骤3: 重启服务

```bash
# 重启Django服务
python manage.py runserver
```

## ✅ 完成！

现在你的网站支持：
- ✉️ 注册时发送验证码到邮箱
- 🔑 找回密码时发送验证码到邮箱
- 🎨 精美的HTML邮件模板
- 🔒 15分钟验证码有效期

## 📝 测试模式（无需配置邮件）

如果还没有配置邮件服务，将 `enabled` 设为 `false` 或删除配置文件：

```json
{
  "enabled": false
}
```

**测试模式特点**：
- API响应中会直接返回验证码
- 验证功能正常工作
- 适合开发调试

## 🔧 常见SMTP配置

### 阿里云邮件推送（推荐）
```json
{
  "smtp_host": "smtpdm.aliyun.com",
  "smtp_port": 465,
  "smtp_use_ssl": true
}
```

### QQ邮箱
```json
{
  "smtp_host": "smtp.qq.com",
  "smtp_port": 465,
  "smtp_use_ssl": true,
  "smtp_password": "授权码（非QQ密码）"
}
```

### 163邮箱
```json
{
  "smtp_host": "smtp.163.com",
  "smtp_port": 465,
  "smtp_use_ssl": true
}
```

## 📚 详细文档

查看 `docs/邮件验证配置说明.md` 了解：
- 完整的API文档
- 数据库模型说明
- 故障排查指南
- 安全性说明

## 🆘 需要帮助？

**邮件发送失败**？检查：
1. SMTP配置是否正确
2. SMTP密码/授权码是否正确
3. 查看日志：`logs/application.log`

**收不到邮件**？检查：
1. 垃圾邮件文件夹
2. 邮箱地址是否正确
3. 邮件服务是否有延迟
