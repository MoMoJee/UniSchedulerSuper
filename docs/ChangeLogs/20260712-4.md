# 2026-07-12 MoMoJee cohort 与无限重复修复

## 问题

MoMoJee 未登记 normalized cohort，浏览器实际仍走 legacy。无截止 DAILY 规则被旧服务预生成未来 90 天的 91 条 JSON 实例，因此 UI 在固定日期后不再显示。

## 修复

- rollout 准入实时核对所有 Planner legacy source row id/checksum；verified 后源漂移立即撤销 normalized 访问。
- `PLANNER_STORAGE_MODE` 默认开启 normalized rollout；无 assignment 用户仍安全回退 legacy。
- MoMoJee strict/parity 再验 0 diff 后登记七个 normalized entrypoint。
- normalized cohort 后端硬阻断所有 legacy Planner JSON 写入。
- 前端改为 entrypoint-aware 模式判断。
- 补齐 Todo 象限拖拽、Reminder 状态/延后/通知/日历操作、课程导入内创建 Group 的 normalized 路径。
- 新增无限 series 跨 2026-11、2027-07 窗口展开且零行增长测试。

## 数据检查

- 用户删除的测试 series 已无 legacy 实例、registry、normalized 行、ID map 或 ChangeSet。
- 删除后 `events` 和 `events_rrule_series` checksum 与 verified state 完全一致。
- 新备份 SHA256：`78F6E30AEBC6676AEE0EA1522CC27F9FEC48D7A2CBB1C35CC260A5A4CAAB29C6`。

## 验证

- core tests：72/72 通过。
- strict verification：0 diff。
- recurrence parity 2020–2035：0 diff。
- 浏览器读取至 2027-07 仍有无限 DAILY occurrence，所有日历窗口请求均为 `/api/v2/events/occurrences/`。
- 数据库翻页前后保持 692 Event、24 Series、0 Override，legacy checksum 不变。
