# 2026-07-12 Planner P1–P3 收尾

## 完成内容

- 完成 P1-C：复杂 RRULE、类型一致性、10,000 slot 窗口、关系/版本/零副作用测试。
- 完成 P2-R：checksum 锁定 repair manifest；MoMoJee 六项 state verified、strict/parity 0 diff；五个废弃测试账号保留 quarantine 并标记 `retired-test-data`。
- 完成 P3-0：统一 rollout policy、稳定 entrypoint 与 `/api/v2/planner/bootstrap/`。
- 完成 P3-C：
  - Event single/all/this-and-future、detach/cancel、override policy 与 lineage；
  - EventGroup、Todo、Reminder normalized CRUD、关系、依赖环检测与 Todo→Event；
  - 搜索、冲突检测、共享 occurrence join；
  - 课程导入事务回滚和稳定指纹幂等。
- 完成 P3-D：日历、Todo、Reminder、Group、Search、Share 的 cohort-aware Web adapter；normalized home GET 不再初始化 legacy Planner 业务行。

## 数据处置

- 生产数据库执行前已创建 SQLite online backup：`backup/20260712/planner_pre_p2r_20260712_030100.sqlite3`。
- SHA256：`6E45E18E59983CAF48832419DEC4F89619C2293F1E48CF58AD103A8AFD9D2E5A`。
- MoMoJee 原始 UserData JSON 未修改；repair 只作用于校验过 checksum 的迁移副本。
- 临时浏览器验收账号及 17 个关联对象已删除。

## 验证

- `DISABLE_EXTERNAL_MCP=1 .venv\Scripts\python.exe manage.py test core.tests -v 1`：69/69 通过。
- `manage.py check`：0 issue。
- `manage.py makemigrations --check --dry-run`：No changes detected。
- 六个改动 JavaScript 文件 `node --check`：通过。
- `logs/planner-direct-userdata-access-20260712-p3-final.json`：`planner_bypass_count=0`。
- MoMoJee strict verification 与 2020–2035 recurrence parity：0 diff。
- 浏览器：normalized Event create/read/search/update/delete 全链路通过。

## 下一阶段

P1–P3 已完成，可以进入 P4：Agent/Quick Action/MCP normalized adapter、精确 rollback 与附件解析。
