# 2026-07-13 · 规范文档 P6 全量校准

## 背景

P1–P6 已完成后，开发规范中仍混杂 `UserData` Planner JSON、`MockRequest`、旧 RRule manager、无限 reversion 回滚、旧 WebSocket Token 认证和 V1 路由示例。虽然部分文件顶部已有迁移提示，但正文会误导后续开发恢复已封存路径。

## 本次更新

- 重写根目录 `AGENT.md`，明确 normalized Planner、Application Service、cohort、V1 410、短期 rollback snapshot 与 Agent 工具边界。
- 重写前端 API、JS、WebSocket 规范和前端索引，规定 `PlannerV2Client`、有限 `[from,to)`、`expected_version`、`occurrence_ref`、附件回滚恢复与 Session WebSocket 认证。
- 重写后端索引、服务层、数据模型、Agent 服务、API 接口规范，移除把 legacy `UserData`、`MockRequest`、`core/services`、`EventsRRuleManager` 或 `@agent_transaction` 作为新 Planner 实现的叙述。
- 更新认证、日志、模板、样式和项目 README，使其匹配 P4 回滚、P5 Feed/CalDAV、P6 cohort/archive 以及实际服务端口。

## 约束结论

1. Planner 新代码只能走 `PlannerExecutionContext → PlannerApplicationService → normalized ORM`。
2. Planner V1 端点认证后固定 410，不允许 fallback。
3. `UserData` Planner key 是只读 archive；新回滚仅保存当前 Agent 窗口内的压缩 aggregate snapshot。
4. 文档中的接口字段与运行示例以 `api_examples/API_REFERENCE.md` 为唯一公开 API 参考。

## 验证

- 对 `README.md`、`AGENT.md`、`docs/前端开发规范/`、`docs/后端开发规范/` 执行旧 Planner 路径、`UserData`、`MockRequest`、`reversion`、WebSocket 认证与 V2 关键字检索。
- 对 Markdown 链接、标题层级、代码块和 Windows 路径进行静态检查。
