# 2026-07-11 Planner P2：旁路导入、严格校验与隔离审计

## 新增能力

- 新增 `core/planner/migration.py`：按用户读取唯一 legacy source 行，构建无副作用计划；`--apply` 时才在单一数据库事务与 reversion revision 中建立 normalized 旁路投影。
- 新增 `migrate_planner_legacy` 命令。默认和 `--dry-run` 都不写数据库，`--apply` 是唯一写入开关；同一 source checksum 重复 apply 会安全跳过。
- 普通 event/todo/reminder、日程组、标签、提醒关系、待办依赖、分享关系、提前提醒与 legacy ID mapping 可导入；未知 JSON 字段保存到对象 metadata。
- 单规则段 event/reminder recurrence 导入为 master + series + EXDATE + sparse reminder state，正常预生成实例仅写 `PlannerLegacyIdMap`，不会再物化为新 event/reminder 行。
- 新增 `PlannerMigrationIssue` 隔离逻辑：非法 RRule、多个规则段、重复/缺失 legacy ID、时间/引用异常、目标冲突和精度损失都会阻止 cohort，而不是用默认值覆盖。
- 新增 `verify_planner_migration` 与 `verify_recurrence_parity`。两者均只读，比较 source checksum、实体字段、legacy ID map 与指定窗口的 occurrence 集；`--strict` 有差异时失败。
- 新增 `core.0010_planner_cohort_assignment`、`PlannerRolloutPolicy` 与 `set_planner_cohort`。用户需同时拥有已验证的所有 source state、零未解决 issue 和明确入口登记；否则即便全局配置为 normalized，策略也回退 legacy。
- 兼容导入会抽取旧式内嵌 `EXDATE`，仅在已物化 occurrence 集唯一证明等价时消解 `COUNT+UNTIL`；完整但缺 ID 的项目使用 source row + index 的确定性兼容 ID。
- 新增 `--skip-quarantined`、`--record-quarantined` 与校验命令的 `--only-imported`，确保隔离用户只留下 state/issue 审计而不写 normalized 业务投影。

## 生产库只读演练

完成了 SQLite 副本 `--apply --skip-quarantined` 演练及生产 shadow 导入：31 位有 Planner source 的用户通过 `verify_planner_migration --only-imported --strict` 和 `verify_recurrence_parity --only-imported --strict`，两份报告均为 0 差异，随后标记为 verified。6 位用户因不可推断字段、规则边界不唯一或失效分享组而被隔离，并只记录 `PlannerMigrationState`/`PlannerMigrationIssue`；2 位用户没有任何 Planner source 行。

`core.0010_planner_cohort_assignment` 已应用；该 migration 仅创建 cohort assignment 表。生产 legacy JSON 未被改写，且没有入口流量切到 normalized 表。

## 验证

- 新增迁移命令测试，覆盖 dry-run 零写入、非法数据预先隔离、普通实体/关系/unknown field、幂等 apply、单规则段 series→master 映射、内嵌 EXDATE、COUNT/UNTIL 语义选择、稳定缺 ID 映射、隔离审计、`verify_planner_migration` 与 `verify_recurrence_parity`。
- 本阶段仍保持 legacy JSON 是唯一业务事实源；P3 之前禁止线上读写切到 normalized 表。

## 后续

1. 仅在 P3 入口适配完成后，为 verified 用户登记 shadow cohort；当前仍为 legacy 读取。
2. 单独处置 6 位 quarantine 用户的 issue；未通过 parity 的用户永远不进入 cohort。
