# 2026-07-13 P5/P6 升级实施与验收规划

- 新增 P5 Feed 与 CalDAV V2 适配详细方案，冻结“只适配原有功能、不扩展完整 CalDAV 协议”的范围。
- P5 规划统一 iCalendar mapper、normalized Feed/CalDAV application 调用链、UID/href 迁移、recurrence resource、ETag/CTag/collection change 和分阶段停机切换。
- 明确 VTODO、Free/Busy、共享 CalDAV、客户端建日历及未实现的 sync-collection 不属于 P5；未实现能力不得在 DAV capability 中宣称。
- 新增覆盖 Feed、discovery、collection、REPORT、GET/PUT/DELETE、recurrence、并发、权限、安全、跨入口和真实客户端的 P5 验收矩阵。
- 新增 P6 Legacy 停写、只读归档与最终验收发布方案，规划 verified 用户全入口 normalized、runtime fallback 关闭和数据库级 Planner key 防写。
- 固化已确认的数据处置：MoMoJee 保持完整验收；User1、test_user、User15、User21、User22 不修复并作为 retired quarantine，禁止回落写 legacy。
- P6 规划全停机备份/migrate/cohort/验证/启动流程、7 天观察期、一个后续生产版本保留期及 P6-F 清理门槛。
- 明确 P6 将移除 `PlannerRolloutPolicy` 的逐请求 legacy checksum 扫描，改为 cutover 封存的 verified manifest；完整源 checksum 仅由离线验证命令复验。
- 更新 Planner 正规化进展文档，加入 P5/P6 详细方案链接、执行顺序和阶段完成定义。

## P5-0 至 P5-B

- 完成 CalDAV 能力基线与 iCalendar identity 冲突审计。
- 新增持久化 UID/href、统一纯 iCalendar mapper，并完成生产迁移及守恒验证。
- HTTP Calendar Feed 对 MoMoJee 切换到 normalized Planner V2 投影，覆盖 Event、Todo、Reminder 与 recurrence。
- 新增 Feed 类型、鉴权、legacy 隔离、只读性和敏感 token 日志脱敏测试；MoMoJee 实库 Feed 验证 712 个 VEVENT 可解析且读取零业务写入。

## P5-C 至 P5-F

- CalDAV discovery、default/group/reminders collection、PROPFIND、calendar-query/multiget、GET 全部切换 normalized query façade。
- VEVENT PUT/DELETE 支持 create/update/group move、RRULE/RDATE/EXDATE、override/cancellation、THISANDFUTURE split 与 If-Match/If-None-Match。
- ETag/CTag 与 `CalendarCollectionVersion`/`CalendarChange` 统一，能力声明和 Allow 头按实际实现收紧。
- 新增生产隔离 CalDAV smoke、跨 Web/Feed/CalDAV 可见性、无限系列 100 次读取零增长和三种鉴权矩阵；P5 最终 171/171。

## P6-0 至 P6-D

- 新增 readiness、批量 cohort promote、archive、no-legacy-write 和 release verify 管理命令。
- 39 个账号全部登记 14 个 Planner entrypoint：34 normalized，User1/test_user/User15/User21/User22 固化为 retired quarantine。
- cohort 运行时改读 sealed manifest；normalized 部署的未登记/隔离入口显式 blocked/423，不再 fallback legacy。
- 新增 `core.0013_planner_legacy_write_guard`：SQLite trigger 防止核心 Planner UserData 增改删和共享旧投影写入；配置类 UserData 与 normalized command 不受影响。
- 157 行、2,788,568 bytes legacy source 已逐行 SHA256 封存；副本演练、生产 migrate、strict/parity、identity/version、integrity/foreign key 和 guard 后 CalDAV 写入均通过。
- P6-D 全量测试 178/178；生产备份位于 `backups/p6-cutover-20260713/db-pre-p6.sqlite3`。

## P6-E 上线观察

- Daphne 已恢复，真实 HTTP/Feed/CalDAV、Agent WebSocket、MoMoJee 浏览器 UI 和隔离账号在线 Event/Todo/Reminder/CalDAV 写烟雾通过。
- 连续 7 天观察从 2026-07-13 14:56 开始；P6-F 受“至少一个后续版本 + 连续 7 天”门槛约束，未提前执行物理清理。
