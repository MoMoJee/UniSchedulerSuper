| # | 问题 | 文件 | 修复方式                                                                               |
|---|---|---|------------------------------------------------------------------------------------|
| 1 | `cleanup_expired` 误删云盘原始文件 | models.py | `hard_delete` 中当 `cloud_file_id is not None` 时跳过文件删除                               |
| 2 | 聊天上传同步绕过配额检查 | sync.py | 同步前先 `quota.can_upload()` 校验，不通过则跳过同步（不影响聊天）                                       |
| 3 | DNS Rebinding SSRF | url_fetcher.py | 新增 `_verify_final_url`；HEAD 后重验目标 IP；GET 改 `allow_redirects=False` 自行跟随重定向并逐跳校验主机  |
| 4 | `download_markdown` SSRF + 内存 DoS | views_api.py | 图片 URL 先过 SSRF 黑名单校验；单图 10MB / 总量 100MB 限制；流式写入替代 `resp.content`                   |
| 5 | 聊天上传双倍磁盘占用 | sync.py | `original_file` 改为直接引用 `session_attachment.file.name`（字符串赋值 = 只存路径，不拷贝文件）          |
| 6 | `rename_folder` 深层路径不一致 | views_api.py | 用 `Replace` DB 函数一次性批量替换后代路径，绕过 `save()` 重算覆盖问题                                    |
| 7 | 配额与文件记录缺乏事务保护 | models.py + views_api.py | `soft_delete` 和 `_core_upload` 的"save + quota"段落包入 `transaction.atomic()`；预解析移至事务外 |
| 8 | `compute_file_hash` 未先 seek(0) | storage.py | 读取前先 `seek(0)`，防止流已被前置操作耗尽导致 hash 计算错误                                             |
| 9 | `pick_files` 隐式截断 100 条 | views_api.py | 移除 `[:100]`；新增 `?q=` 关键词过滤参数                                                       |
| 10 | `ensure_path` 并发竞态 IntegrityError | models.py | `get_or_create` 外加 `except IntegrityError` 降级到 `get`，防止并发创建同名目录崩溃                  |
| 11 | Agent 的 system prompt 在构建时存在把时间戳放在最前面的致命错误 | agent.py | 现在暂时挪到了后面，后续需要进一步增强构建流程                                                            |