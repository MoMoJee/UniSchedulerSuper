psutil

django>=5.1.8
django-cors-headers>=4.6.0
djangorestframework>=3.14.0
django-reversion

openai

anthropic>=0.36.0
langchain-openai

langchain-anthropic
langgraph-checkpoint-sqlite
langchain-mcp-adapters
langgraph==1.0.4

# 联网搜索服务
tavily-python

pytz>=2024.1
markdown
icalendar
requests
python-dateutil>=2.8.0
cryptography>=41.0.0

concurrent_log_handler
daphne
channels

# 附件系统 - OCR & 图片处理  & 文档解析 & 语音转文字，可以选择性安装
Pillow
pytesseract  # 需要系统安装 Tesseract-OCR
easyocr  # 可选，约 1GB，更高精度的 OCR

pdfplumber      # PDF 解析
python-docx     # Word 解析
openpyxl        # Excel 解析

mutagen         # 非 WAV 格式时长检测
pydub           # 非标准 WAV 格式转换（需 ffmpeg）
faster-whisper  # 本地语音识别兜底